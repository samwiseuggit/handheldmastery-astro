<div id="ad-nat-slot" class="ad-nat-slot">
  <span id="ad-nat-label" class="ad-nat-tag">Sponsored</span>
</div>

<script is:inline>
  (function() {
    var slot = document.getElementById('ad-nat-slot');
    var label = document.getElementById('ad-nat-label');
    if (!slot) return;

    if (window.matchMedia('(min-width: 768px)').matches) {
      if (label) label.textContent = 'Sponsored';
      slot.style.minHeight = '100px';

      var nativeScript = document.createElement('script');
      nativeScript.async = true;
      nativeScript.setAttribute('data-cfasync', 'false');
      nativeScript.src = 'https://pl29763344.effectivecpmnetwork.com/8b7696e02f356d6562fb7d33ae23ef75/invoke.js';
      slot.appendChild(nativeScript);

      var nativeContainer = document.createElement('div');
      nativeContainer.id = 'container-8b7696e02f356d6562fb7d33ae23ef75';
      slot.appendChild(nativeContainer);
    } else {
      if (label) label.textContent = 'Advertisement';
      slot.style.minHeight = '300px';

      var key = '1a4f783befba01b8d47aede5beabd340';
      var url = 'https://www.highperformanceformat.com/' + key + '/invoke.js';

      var iframe = document.createElement('iframe');
      iframe.style.width = '160px';
      iframe.style.height = '300px';
      iframe.style.border = 'none';
      iframe.style.overflow = 'hidden';
      iframe.scrolling = 'no';
      iframe.loading = 'eager';
      iframe.title = 'Footer advertisement';

      slot.appendChild(iframe);

      var doc = iframe.contentDocument || iframe.contentWindow.document;
      doc.open();
      doc.write(`<!DOCTYPE html><html><head><style>body{margin:0;padding:0;overflow:hidden;background:transparent}</style></head><body><script>atOptions={"key":"${key}","format":"iframe","height":300,"width":160,"params":{}};<\/script><script src="${url}"><\/script></body></html>`);
      doc.close();
    }
  })();
</script>

<style>
  .ad-nat-slot {
    margin: 0;
    padding: 16px;
    background: #0f172a;
    border-top: 1px solid #1e293b;
    border-bottom: 1px solid #1e293b;
    text-align: center;
    min-height: 100px;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ad-nat-tag {
    font-size: 10px;
    color: #475569;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    position: absolute;
    top: 2px;
    right: 4px;
    z-index: 1;
  }

  @media (max-width: 767px) {
    .ad-nat-slot {
      min-height: 300px;
    }
  }
</style>
